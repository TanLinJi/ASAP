# E3.4 — External published point-cloud purifier (optional)

| Field | Value |
|-------|-------|
| **Experiment ID**         | `E3.4` |
| **Family**                | `E3_baseline_defenses` |
| **Short name**            | `external_published` |
| **Owner**                 | core team |
| **Status**                | **optional** |
| **Last update**           | 2026-05-20 |
| **Linked paper section**  | `paper/04_experiments.md` §4.2 *Baselines* (the "scene-wide diffusion purifier" bullet); referenced as `[@zhang2023ada3diff]` in the paper (Ada3Diff, Zhang et al., ACM MM 2023). |
| **Linked code / configs** | TBD; depends on which external method is chosen and whether a checkpoint is publicly available. |

---

## 1. Purpose / hypothesis

If page budget allows, include **one** representative published scene-wide diffusion purifier (e.g. Ada3Diff or PointDP) as an external reference, to show that ASAP's *local + selective* design improves over the *scene-wide* design of prior diffusion-based defenses.

## 2. Dependencies

- `E1.x` clean baselines.
- `E2.x` attacked sets.
- A reproducible implementation of the chosen external purifier (likely vendored under `third_party/`).

## 3. Setup

### 3.1 To-be-decided

- Pick the external method (Ada3Diff is the current placeholder).
- Confirm its license is compatible with ASAP's intended release.
- Confirm a pre-trained checkpoint is available; otherwise this experiment is dropped.

### 3.2 Output layout (planned)

```text
outputs/defenses/<dataset>_<detector>/<attack>/E3.4_external_<name>/
```

### 3.3 Reproducible command (planned)

*(To be written once the external method is chosen.)*

## 4. Procedure

1. Decide on the external method and its release status.
2. Vendor it into `third_party/`.
3. Run on every (detector, attack) attacked val set.
4. Record AP / ASR per cell.

## 5. Expected output

- One extra column in `paper/04_experiments.md` Table 1 for external reference (or one extra supplementary table if the main table is full).

## 6. Actual results

*(To be filled in if this experiment is executed.)*

## 7. Analysis

*(To be filled in.)*

## 8. Issues encountered & resolutions

*(To be filled in.)*

## 9. Next actions

- Only start this experiment after MS3 lands and the paper's main rows are stable; this is the lowest-priority experiment in the plan.

## 10. Change log

| Date | Author | Change |
|------|--------|--------|
| 2026-05-20 | core team | Skeleton (optional). |
