name: asap
channels:
  - defaults
dependencies:
  - python=3.9
  - pip
  - pip:
      - torch==2.5.1+cu124
      - torchvision==0.20.1+cu124
      - --extra-index-url https://download.pytorch.org/whl/cu124
      - numpy<2
      - llvmlite
      - numba
      - tensorboardX
      - easydict
      - pyyaml
      - scikit-image
      - tqdm
      - SharedArray
      - opencv-python
      - pyquaternion
      - spconv-cu120
